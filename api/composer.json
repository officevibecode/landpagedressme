{
    "name": "tgoo/dressme-api",
    "description": "API backend para processamento de pagamentos do DressMe via Stripe",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "stripe/stripe-php": "^13.0"
    },
    "autoload": {
        "psr-4": {
            "Api\\": "src/"
        }
    },
    "config": {
        "vendor-dir": "stripe-php"
    }
}

